---
description: 远程开发默认本机完成；用户点名时可委派指定模型
alwaysApply: true
---

# 远程开发工作方式

本仓库当前在自有服务器远程开发：`187.127.218.157`，工作区 `/opt/xxxtg`。

- 自己读代码、改代码、跑测试、重建 Docker、在浏览器或 curl 验证。
- 默认由当前对话模型直接完成；**用户明确要求委派时**，可用 `Task` 交给指定模型（如 Grok 4.6）做审计/实现。
- 与用户用简体中文沟通；不要把密钥写入 Git。
- 未要求时不要 commit / push。
- **默认只在统一工作分支 `cursor/reghelp-push-refund-9abd` 上改代码并 push**；禁止为每个小改动新开一堆 feature 分支导致测试/运行代码分裂。仅当用户明确要求独立 PR 时才另开分支，并尽快合回该工作分支。
- 这台机器的 Remote Control worker 名是 `xxxtg@srv1884560`；手机/Web 遥控走该 worker。

```text
❌ BAD: 未经用户同意就乱开一堆分支 / 把密钥写进 Git
✅ GOOD: 统一工作分支上改；用户点名委派时再用 Task(指定模型)
```
